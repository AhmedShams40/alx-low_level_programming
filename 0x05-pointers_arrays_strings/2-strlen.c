#include "main.h"

/**
 * _strlen - a function that swaps the values of two integers.
 * @s: input string.
 * Return: no return.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{}

	return (i);
}
